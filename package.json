{
  "name": "pizzamine-angular",
  "version": "1.0.0",
  "engines": {
      "node": ">=18 <19"
  },
  "scripts": {
      "start": "cd backend/built && node server.js",
      "build": "cd backend && npm run build",
      "prebuild": "cd frontend && npm run build"
  }
}